<%= form_with model: @behaviour, local: true do |form| %>

  <% if @behaviour.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@behaviour.errors.count, "error") %> prohibited
        this behaviour from being saved:
      </h2>
      <ul>
        <% @behaviour.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= form.label :title %><br>
    <%= form.text_field :title %>
  </p>

  <p>
    <%= form.label :text %><br>
    <%= form.text_area :text %>
  </p>

  <p>
    <%= form.submit %>
  </p>

<% end %>
